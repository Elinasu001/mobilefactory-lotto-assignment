<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobilefactory.lotto.sms.model.dao.SmsLogMapper">

    <insert id="insertSmsLog" parameterType="com.mobilefactory.lotto.sms.model.vo.SmsLog">
        <selectKey keyProperty="smsId" resultType="long" order="BEFORE">
            SELECT SEQ_SMS_LOG.NEXTVAL FROM DUAL
        </selectKey>
        INSERT
          INTO
               MF_SMS_LOG
               (
               SMS_ID
             , PARTICIPANT_ID
             , SMS_TYPE
             , MESSAGE_CONTENT
             , SEND_STATUS
               )
               VALUES
               (
               #{smsId}
             , #{participantId}
             , #{smsType}
             , #{messageContent}
             , #{sendStatus}
               )
    </insert>

</mapper>